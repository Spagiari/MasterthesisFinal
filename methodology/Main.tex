    % Place nodes
    \node [block] (init) {Início};
    \node [block, below of=init] (readimages) {Ler imagens};
    \node [block, right of=readimages] (spotdetect) {Detectar manchas solares};
    \node [block, right of=spotdetect] (groupdetect) {Detectar grupos de manchas solares};
    \node [block, right of=groupdetect] (avgintensity) {Intensidade média do centro do Sol};
    \node [block, below of=avgintensity] (spotsarea) {Área das manchas detectadas};
    \node [block, left of=spotsarea] (temp) {Temperatura das manchas solares};
    \node [block, left of=temp] (magnetogram) {Campo magnético das manchas detectadas};
    \node [decision, below of=magnetogram] (decide) {Existem mais imagens?};
    \node [block, below of=decide, node distance=3.5cm] (fim) {Fim};
    % Draw edges
    \path [line] (init) -- (readimages);
    \path [line] (readimages) -- (spotdetect);
    \path [line] (spotdetect) -- (groupdetect);
    \path [line] (groupdetect) -- (avgintensity);
    \path [line] (avgintensity) -- (spotsarea);
    \path [line] (spotsarea) -- (temp);
    \path [line] (temp) -- (magnetogram);
    \path [line] (magnetogram) -- (decide);
    \path [line] (decide) -| node  {sim} (readimages);
    \path [line] (decide) -- node {não}(fim);
